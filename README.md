My personal config for Neovim original from kickstart.nvim

## Clone Instructions

To clone this repository to the default Neovim configuration directory (%USERPROFILE%\AppData\Local\nvim on Windows), use the following command:

